{% extends 'base.html.twig' %}

{% block title %}Privatisation{% endblock %}

{% block body %}
    <h1>Privatisation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ privatisation.id }}</td>
            </tr>
            <tr>
                <th>Capacite</th>
                <td>{{ privatisation.capacite }}</td>
            </tr>
            <tr>
                <th>Surface</th>
                <td>{{ privatisation.surface }}</td>
            </tr>
            <tr>
                <th>Service</th>
                <td>{{ privatisation.service }}</td>
            </tr>
            <tr>
                <th>Annonce</th>
                <td>{{ privatisation.annonce }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('privatisation_index') }}">back to list</a>

    <a href="{{ path('privatisation_edit', {'id': privatisation.id}) }}">edit</a>

    {{ include('privatisation/_delete_form.html.twig') }}
{% endblock %}
